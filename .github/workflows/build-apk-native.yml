name: Build APK Native (Sans Expo)

on:
  # Déclenchement manuel
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (e.g., 1.7.2)'
        required: true
        default: '1.7.2'
  
  # Déclenchement sur push de tags
  push:
    tags:
      - 'v1.7.*'
      - 'v2.*'
      - '7.*'
      - 'native-build-*'

jobs:
  build-android-native:
    name: 🚀 Build Android APK (Native)
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      packages: write
      actions: read
      checks: write
      deployments: write
      issues: write
      pull-requests: write
      repository-projects: write
      statuses: write
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: 🏷️ Get version info
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "📱 Building version: $VERSION"
      
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: 🤖 Setup Android SDK
        uses: android-actions/setup-android@v3
      
      - name: 📦 Install dependencies
        run: |
          echo "📦 Installing dependencies..."
          npm ci
      
      - name: 🔑 Setup Expo without EAS
        run: |
          echo "🔑 Installing Expo CLI..."
          npm install -g expo-cli
          # Pas besoin de login pour prebuild
      
      - name: 🏗️ Prebuild Android native files
        run: |
          echo "🏗️ Generating Android native files..."
          npx expo prebuild --platform android --clean
          
          echo "✅ Android files generated"
          ls -la android/
          
          echo "🔍 Vérification network_security_config..."
          if [ -f "android/app/src/main/res/xml/network_security_config.xml" ]; then
            echo "✅ network_security_config.xml créé par le plugin"
            cat android/app/src/main/res/xml/network_security_config.xml | head -10
          else
            echo "⚠️ network_security_config.xml PAS créé"
            ls -la android/app/src/main/res/xml/ || echo "⚠️ xml directory missing"
          fi
      
      - name: 🔧 Configure Gradle
        run: |
          echo "🔧 Configuring Gradle..."
          cd android
          
          # Créer local.properties si nécessaire
          if [ ! -f "local.properties" ]; then
            echo "sdk.dir=$ANDROID_HOME" > local.properties
            echo "✅ Created local.properties"
          fi
          
          # Vérifier gradle wrapper
          if [ -f "gradlew" ]; then
            chmod +x gradlew
            echo "✅ Gradle wrapper ready"
          else
            echo "❌ No gradle wrapper found"
            exit 1
          fi
      
      - name: 🔨 Build APK with Gradle
        run: |
          echo "🔨 Building APK..."
          cd android
          
          # Build release APK
          ./gradlew assembleRelease --no-daemon --stacktrace
          
          echo "✅ APK built successfully"
          
          # Trouver l'APK
          find app/build/outputs/apk/release/ -name "*.apk" -type f
      
      - name: 📝 Rename APK
        run: |
          echo "📝 Renaming APK..."
          
          VERSION="${{ steps.version.outputs.version }}"
          
          # Trouver l'APK généré
          APK_PATH=$(find android/app/build/outputs/apk/release/ -name "*.apk" -type f | head -1)
          
          if [ -n "$APK_PATH" ]; then
            # Copier et renommer
            cp "$APK_PATH" "roleplay-chat-v${VERSION}-native.apk"
            
            APK_SIZE=$(du -h "roleplay-chat-v${VERSION}-native.apk" | cut -f1)
            echo "✅ APK renamed: roleplay-chat-v${VERSION}-native.apk ($APK_SIZE)"
            
            ls -lh "roleplay-chat-v${VERSION}-native.apk"
          else
            echo "❌ No APK found"
            exit 1
          fi
      
      - name: 📤 Upload APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: roleplay-chat-v${{ steps.version.outputs.version }}-native-apk
          path: roleplay-chat-v${{ steps.version.outputs.version }}-native.apk
          retention-days: 90
      
      - name: 🏷️ Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: roleplay-chat-v${{ steps.version.outputs.version }}-native.apk
          body: |
            # 🔥 Roleplay Chat v${{ steps.version.outputs.version }} - Images NSFW Réalistes!
            
            **Build Method**: Native Android (Gradle)  
            **No Expo Account Required**: ✅  
            **Completely Free**: ✅
            
            ## 🚀 Nouveautés v2.2.0
            
            ### 📸 Images NSFW Réalistes
            - **85% style photo-réaliste** - Fini les images cartoon!
            - **Tenues sexy variées** - Lingerie, bikini, nu artistique...
            - **Poses sensuelles** - Poses suggestives et explicites
            - **Mode Sexy vs Revealing** - 2 niveaux d'intensité NSFW
            
            ### ⚡ Ollama Plus Rapide
            - **Timeout réduit à 30s** (au lieu de 2 min)
            - **Fallback automatique** vers Groq si trop lent
            - **Messages d'erreur clairs** avec solutions
            
            ### 🎨 Génération d'Images Simplifiée
            - **Config automatique** Freebox + Pollinations
            - **Message clair** si SD Local non disponible
            - **Qualité maximale** 768x768 pixels
            
            ## 🔥 3 Modes de Conversation (18+)
            - **😊 Normal (SFW)** - Conversations amicales
            - **💕 Romance** - Flirt, baisers, câlins
            - **🔥 Spicy** - Contenu adulte explicite!
            
            ## 📥 Installation
            
            1. Téléchargez l'APK
            2. Installez sur Android
            3. Configurez vos clés API (Paramètres)
            4. Activez le mode Romance ou Spicy dans votre profil
            5. Profitez des images réalistes! 🔥
            
            ## 🎯 Configuration Images
            
            | Source | Recommandation |
            |--------|----------------|
            | Freebox + Pollinations | ⭐⭐⭐⭐⭐ Parfait |
            | Pollinations seul | ⭐⭐⭐⭐ Bon |
            
            ---
            
            **Build**: Native Gradle  
            **Date**: ${{ github.event.head_commit.timestamp }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: 📊 Build Summary
        if: always()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" >> $GITHUB_STEP_SUMMARY
          echo "## 📱 Native Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **App**: Roleplay Chat" >> $GITHUB_STEP_SUMMARY
          echo "- **Version**: ${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Platform**: Android" >> $GITHUB_STEP_SUMMARY
          echo "- **Build Method**: Native Gradle (No Expo Account)" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch**: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit**: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ job.status }}" = "success" ]; then
            echo "### ✅ Build réussi!" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📥 **APK disponible dans les artifacts**" >> $GITHUB_STEP_SUMMARY
            echo "🆓 **Build gratuit - Pas de quota Expo**" >> $GITHUB_STEP_SUMMARY
          else
            echo "### ❌ Build échoué" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Consultez les logs ci-dessus pour plus de détails." >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" >> $GITHUB_STEP_SUMMARY
